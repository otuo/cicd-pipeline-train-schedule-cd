pipeline {
    agent any
    stages {
        stage('Testing') {
            steps {
                echo 'Running build automation'
            }
        }
        stage ('Build') {
            steps {
                sh 'ls -al'
            }   
        }
    }
}
